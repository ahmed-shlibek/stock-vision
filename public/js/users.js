/**
 * StockVision - User Management Script
 */

document.addEventListener('DOMContentLoaded', () => {
    // User management specific logic can be added here
    // Currently, delete confirmations are handled inline in the views
});
